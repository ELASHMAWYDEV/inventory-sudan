import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF2196F3);
  static const primaryLight = Color(0xFF64B5F6);
  static const primaryDark = Color(0xFF1976D2);

  // Secondary colors
  static const secondary = Color(0xFF4CAF50);
  static const secondaryLight = Color(0xFF81C784);
  static const secondaryDark = Color(0xFF388E3C);

  // Accent colors
  static const accent = Color(0xFFFFC107);
  static const accentLight = Color(0xFFFFD54F);
  static const accentDark = Color(0xFFFFA000);

  // Text colors
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textLight = Color(0xFFFFFFFF);

  // Background colors
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const error = Color(0xFFD32F2F);

  // Border colors
  static const border = Color(0xFFE0E0E0);

  // Status colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const info = Color(0xFF2196F3);
  static const danger = Color(0xFFD32F2F);

  // Process colors
  static const farmToDrying = Color(0xFF4CAF50);
  static const packaging = Color(0xFFFFC107);
  static const sales = Color(0xFF2196F3);
  static const stockLog = Color(0xFF9C27B0);
}
